$(document).ready(function(){
    $('.carrossel').slick({
        autoplay: true, // Iniciar a reprodução automática
        autoplaySpeed: 1000, // Velocidade da reprodução automática (em milissegundos)
        dots: true, // Mostrar indicadores de navegação (pontos)
    });
});